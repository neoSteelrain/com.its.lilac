<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">
    <select id="checkDuplicatedEmail" parameterType="String" resultType="int">
        select count(member_id) as emailCount from tbl_member where member_email = #{email};
    </select>

    <insert id="userRegistration" parameterType="user">
        insert into tbl_member(member_nickname,
                               member_email,
                               member_desc,
                               member_password,
                               member_profile,
                               member_address,
                               member_grade) values
            (#{member_nickname}, #{member_email}, #{member_desc}, #{member_password}, #{member_profile}, #{member_address}, #{member_grade});
    </insert>

    <select id="userLogIn" parameterType="String" resultType="user">
        select member_id,
               member_nickname,
               member_email,
               member_desc,
               member_password,
               member_profile,
               member_address,
               member_date,
               member_grade
        from tbl_member
        where member_email = #{email} and member_password = #{password};
    </select>
</mapper>